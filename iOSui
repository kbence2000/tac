<!DOCTYPE html>
<html lang="en">
<head>

  <!-- iOS FULL SUPPORT -->
  <meta charset="UTF-8" />
  <meta name="viewport"
        content="width=device-width, initial-scale=1.0,
                 maximum-scale=1.0, user-scalable=no,
                 viewport-fit=cover" />

  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-touch-fullscreen" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />

  <!-- iOS splashscreen safe-area fix -->
  <style>
    :root {
      /* iOS safe areas */
      --safe-top: env(safe-area-inset-top);
      --safe-bottom: env(safe-area-inset-bottom);
      --safe-left: env(safe-area-inset-left);
      --safe-right: env(safe-area-inset-right);

      --bg: #0f0f15;
      --card: #181824;
      --accent: #7b5cff;
      --text: #f4f4ff;
    }

    body {
      margin: 0;
      padding:
        calc(var(--safe-top) + 16px)
        calc(var(--safe-right) + 16px)
        calc(var(--safe-bottom) + 16px)
        calc(var(--safe-left) + 16px);

      background: var(--bg);
      font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Text',
                   'Inter', 'Roboto', sans-serif;
      color: var(--text);
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }

    /* Smooth scroll, physics feel on iOS */
    html, body {
      height: 100%;
      overscroll-behavior-y: none;
      -webkit-overflow-scrolling: touch;
    }

    /* UNIVERSAL mobile container */
    .app-container {
      max-width: 480px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    /* Card UI */
    .card {
      background: var(--card);
      padding: 20px;
      border-radius: 20px;
      box-shadow: 0 0 25px rgba(0,0,0,0.25);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      transition: 0.25s;
    }
    .card:hover {
      transform: translateY(-3px);
    }

    /* Button */
    .btn {
      background: var(--accent);
      padding: 16px;
      border-radius: 14px;
      font-weight: bold;
      text-align: center;
      color: white;
      cursor: pointer;
      transition: 0.2s;
      user-select: none;
      -webkit-user-select: none;
    }
    .btn:active { transform: scale(0.97); }

    /* Animated header */
    .title {
      font-size: 30px;
      font-weight: 800;
      text-align: center;
      margin-top: 10px;
      background: linear-gradient(120deg, #ab7cff, #7b5cff);
      -webkit-background-clip: text;
      color: transparent;
    }

    /* Input fields */
    .input {
      width: 100%;
      background: #111;
      border: 1px solid #333;
      padding: 14px;
      border-radius: 14px;
      color: #eee;
      font-size: 16px;
      outline: none;
      transition: 0.2s;
    }
    .input:focus {
      border-color: var(--accent);
    }

    /* Mobile tabs / navbar */
    .navbar {
      position: fixed;
      bottom: calc(var(--safe-bottom) + 10px);
      left: 50%;
      transform: translateX(-50%);
      background: rgba(20,20,30,0.9);
      padding: 12px 22px;
      border-radius: 30px;
      display: flex;
      gap: 30px;
      box-shadow: 0 0 40px rgba(0,0,0,0.35);
      backdrop-filter: blur(15px);
    }
    .nav-item {
      color: #bbb;
      font-size: 20px;
      transition: 0.2s;
    }
    .nav-item.active {
      color: var(--accent);
    }

    /* Fade animation */
    .fade {
      animation: fadeIn 0.4s ease forwards;
      opacity: 0;
    }
    @keyframes fadeIn {
      to { opacity: 1; }
    }

  </style>

  <title>iOS Ready Mobile UI</title>
</head>

<body>

  <div class="app-container fade">

    <h1 class="title">📱 Mobile iOS-Ready App</h1>

    <div class="card">
      <h2 style="margin-top:0;">Login</h2>
      <input class="input" placeholder="Email" />
      <input class="input" type="password" placeholder="Password" />
      <div class="btn" onclick="alert('Logged in!')">Login</div>
    </div>

    <div class="card">
      <h2 style="margin-top:0;">Live Status</h2>
      <p id="status">Loading device info...</p>
      <div class="btn" onclick="refreshStatus()">Refresh</div>
    </div>

    <div class="card">
      <h2 style="margin-top:0;">Action Center</h2>
      <div class="btn" onclick="runAction()">🚀 Run Quick Action</div>
    </div>

  </div>

  <!-- Bottom Mobile Navbar -->
  <div class="navbar">
    <div class="nav-item active">🏠</div>
    <div class="nav-item">⚙️</div>
    <div class="nav-item">👤</div>
  </div>

  <script>
    // Mobile-focused JS actions

    function refreshStatus() {
      const device = navigator.userAgent;
      const time = new Date().toLocaleTimeString();
      document.getElementById("status").innerText =
        "Device: " + device + "\nTime: " + time;
    }
    refreshStatus();

    function runAction() {
      const btn = document.querySelector(".btn");
      btn.style.background = "#5cffba";
      setTimeout(() => btn.style.background = "var(--accent)", 300);
      alert("Action executed!");
    }
  </script>

</body>
</html>
